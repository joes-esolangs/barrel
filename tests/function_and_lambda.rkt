#lang barrel
` these programs both do the same thing
` (taking a number of stdin, convert it into a number, cube it and then print it)
` except one uses a named function, and one uses a lambda
,![::**]!.
{::**},!a.